{% extends 'base.html.twig' %}

{% block title %}Edit Seeder{% endblock %}

{% block body %}
    <h1>Edit Seeder</h1>

    {{ include('seeder/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_seeder_index') }}">back to list</a>

    {{ include('seeder/_delete_form.html.twig') }}
{% endblock %}
