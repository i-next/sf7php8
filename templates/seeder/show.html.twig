{% extends 'base.html.twig' %}

{% block title %}Seeder{% endblock %}

{% block body %}
    <h1>Seeder</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ seeder.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ seeder.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_seeder_index') }}">back to list</a>

    <a href="{{ path('app_seeder_edit', {'id': seeder.id}) }}">edit</a>

    {{ include('seeder/_delete_form.html.twig') }}
{% endblock %}
