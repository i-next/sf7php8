{% extends 'base.html.twig' %}

{% block title %}MySeeds{% endblock %}

{% block body %}
    <h1>MySeeds</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ my_seed.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ my_seed.name }}</td>
            </tr>
            <tr>
                <th>Seeder</th>
                <td>{{ my_seed.seeder }}</td>
            </tr>
            <tr>
                <th>Quantity</th>
                <td>{{ my_seed.quantity }}</td>
            </tr>
            <tr>
                <th>Duration</th>
                <td>{{ my_seed.duration }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ my_seed.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_my_seeds_index') }}">back to list</a>

    <a href="{{ path('app_my_seeds_edit', {'id': my_seed.id}) }}">edit</a>

    {{ include('my_seeds/_delete_form.html.twig') }}
{% endblock %}
