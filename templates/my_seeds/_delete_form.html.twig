<form method="post" action="{{ path('app_my_seeds_delete', {'id': my_seed.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ my_seed.id) }}">
    <button class="btn">Delete</button>
</form>
